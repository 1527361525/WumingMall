<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wlyykf.mall.mappers.CartItemMapper">
    <select id="getTotalPrice" resultType="java.math.BigDecimal">
        SELECT IFNULL(SUM(total_price), 0.00)
        FROM tb_cart_item
        WHERE user_id = #{userId}
        AND selected = 1
        AND del_flag = 0
    </select>

    <select id="getAll" resultType="com.wlyykf.mall.vo.CartItemVO">
        SELECT
            id AS cartItemId,
            product_id AS productId,
            product_name AS productName,
            product_image AS productImage,
            price,
            total_price AS totalPrice,
            quantity,
            selected
        FROM tb_cart_item
        WHERE user_id = #{userId}
        AND del_flag = 0
    </select>

    <select id="getAllList" resultType="com.wlyykf.mall.vo.CartItemVO">
        SELECT
            id AS cartItemId,
            product_id AS productId,
            product_name AS productName,
            product_image AS productImage,
            price,
            total_price AS totalPrice,
            quantity,
            selected
        FROM tb_cart_item
        WHERE user_id = #{userId}
          AND del_flag = 0
    </select>
</mapper>
